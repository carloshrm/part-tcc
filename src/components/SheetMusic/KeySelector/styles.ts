import { Select } from "antd";
import styled from "styled-components";

export const KeySelect = styled(Select<string>)`
  width: 100%;
`;

export const KeyOptionLabel = styled.span`
  text-transform: capitalize;
`;
